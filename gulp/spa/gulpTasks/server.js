const gulp = require('gulp');

function monitorarArquivos(callback){
    return callback()
}

function server(callback){
    return callback()
}

exports.default = {
    monitorarArquivos,
    server
}